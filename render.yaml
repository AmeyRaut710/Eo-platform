services:
  - type: webService
    name: eo-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    plan: starter
    region: oregon
    autoDeploy: true

  - type: webService
    name: eo-titiler
    env: docker
    dockerfilePath: backend/titiler.Dockerfile
    plan: starter
    region: oregon
    autoDeploy: true
